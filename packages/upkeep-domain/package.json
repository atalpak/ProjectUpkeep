{
  "name": "@upkeep/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-free product rules shared by the web app and mobile — currently the stacking-decision policy and the card-instance vocabulary it decides over.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "tsx": "^4.23.0",
    "@types/node": "^22.15.0"
  }
}
